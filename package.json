{
  "name": "@instantdomain/bandit",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/InstantDomain/instant-bandit"
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./server": "./dist/server.js"
  },
  "files": [
    "dist/index.js",
    "dist/server.js",
    "dist/lib",
    "dist/components",
    "docker-compose.dev.yml"
  ],
  "scripts": {
    "dc": "docker-compose -f docker-compose.dev.yml --env-file .env.local",
    "up": "yarn dc up -d",
    "rc": "yarn dc exec redis redis-cli",
    "rct": "yarn dc exec redis-testing redis-cli",
    "dev": "NODE_OPTIONS='--inspect=localhost:9229' next dev && yarn dc up -d",
    "compile": "tsc -p tsconfig.publish.json",
    "minify": "swc ./dist -s false -d ./dist",
    "build": "yarn clean && yarn compile && yarn minify",
    "start": "next start",
    "test": "jest --verbose",
    "clean": "rm -rf dist",
    "pack:test": "yarn build && yarn pack --dry-run && tar tvf instantdomain-bandit-v0.1.0.tgz"
  },
  "dependencies": {
    "ioredis": "^5.0.6",
    "tslib": "^2.4.0"
  },
  "peerDependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.198",
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.2.0",
    "@types/ioredis": "^4.28.10",
    "@types/jest": "^26.0.23",
    "@types/node": "^15.6.1",
    "@types/react": "^18.0.9",
    "babel-jest": "^27.0.2",
    "jest": "^27.0.3",
    "jest-fetch-mock": "^3.0.3",
    "jest-localstorage-mock": "^2.4.14",
    "next": "^12.1.6",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "typescript": "^4.3.2"
  }
}
